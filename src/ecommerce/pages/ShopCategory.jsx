// import { useContext } from "react"
// import { ShopContext } from "../context/ShopContext"
// import Item from "../components/items/Item";

const ShopCategory = (props) => {
  // const {Popular} = useContext(ShopContext);
  // console.log(props.category)

  return (
    <div>
      {/* {Popular.map((event) =>{
        console.log(event.category.name)
        if(props.category === event.category.name){
          
          return <Item key={item.id} image = {event.images[0]} name={event.title} price={event.price} event={item.category} />
        }
        else{
            return null;
        }
      })}
       */}
    </div>
  )
}

export default ShopCategory